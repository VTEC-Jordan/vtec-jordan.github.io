# OS & editor artifacts
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
.vscode/
.idea/

# Build / temp outputs
*.min.css
*.min.js
dist/
build/

# Sensitive / internal dev files — never commit these
AUDIT.md
*-audit.md
*.secret
.env
.env.*

# Local tooling (npm + shadcn/ReactBits registry config for Claude Code MCP)
node_modules/
package.json
package-lock.json
components.json
.mcp.json
*.log

# AI agent tooling — internal, must never be published to the site
.agents/
.claude/
